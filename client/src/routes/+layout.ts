// SPDX-License-Identifier: LicenseRef-FSL-1.1-Apache-2.0
// Disable SSR — ShellWatch is a client-side SPA
// Static adapter with fallback to index.html for client-side routing
export const ssr = false;
export const prerender = false;
